
!Смотрите function sortTable(table_id){...}

по-моему, проблема с этим 
for (let k = 0; k < key_array.length; k++) {
                table_object[key_array[k]] = every_row_value[k];//объект
            }
!Проблема: в массив пушится только последний объект(данные из последнего ряда таблицы)
1.берем любую таблицу по id (не данные из массива или объекта)
2. собираем значения названий столбцов в массив.
Они послужат ключами к объектам
3. перебираем ряд таблицы и из каждой ячеки записываем значения в массив
   после перебора ряда записываем в объект ключ (из массива с ключасм)
 и значение из массива перебранного ряда
4. в массиве должно появиться столько объектов - сколько рядов в 
таблице не считая первого (названия столбцов)
5. отрисовываем таблицу сортируя кликнутую колонку